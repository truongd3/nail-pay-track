import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  section: { marginBottom: 28 },
  label: {
    fontSize: 12,
    fontWeight: '700',
    color: '#9a9a9a',
    letterSpacing: 1,
    marginBottom: 10,
  },
  card: {
    backgroundColor: '#fff',
    borderRadius: 18,
    overflow: 'hidden',
  },
});