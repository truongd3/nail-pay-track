import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    borderRadius: 16,
    padding: 18,
    marginBottom: 14,
  },
  label: {
    fontSize: 11,
    color: '#9a9a9a',
    letterSpacing: 1,
    marginBottom: 8,
  },
  input: {
    fontSize: 18,
    color: '#1a1a2e',
    padding: 0,
  },
});