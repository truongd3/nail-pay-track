import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  weekday: {
    fontSize: 15,
    fontWeight: '600',
    color: '#1a1a2e',
  },
  date: {
    fontSize: 13,
    color: '#9a9a9a',
    marginTop: 2,
  },
});